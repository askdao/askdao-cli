# internal/webstudio/
> L2 | 父级: ../../CLAUDE.md

本地 Web 工作台 —— `askdao agent edit` 拉起的 `127.0.0.1` HTTP server + `go:embed` 自包含前端。KOL 在浏览器审阅/编辑 Agent spec（profile + persona + 主题色）、按 scope 分组勾选 skill/MCP，点 Deploy/Done 一站式。写 yaml / deploy 由 cmd 层注入回调，webstudio **不依赖** pipeline/deploy。

## 成员清单

- **server.go** — `Options` / `Serve` / `Handler`（桌面长驻宿主复用 —— 同 Serve 路由但不「deploy 即退出」，供 Wails `AssetServer.Handler` 挂载）/ `openBrowser`。`net.Listen("tcp","127.0.0.1:0")` 随机端口；`go:embed studio.html` + `go:embed logo.png`；路由 `/`（HTML）+ `/logo.png`（AskDAO 品牌 logo，image/png）+ `/api/spec`(GET StudioData) + `/api/save`（写 yaml，不退出）+ `/api/deploy`（写 yaml + deploy，回 `DeployResult`{message/group_link/agent_id/created}，成功退出）+ `/api/done`（**Save & finish**：写 yaml + 退出，不 deploy；同 /api/save 调 OnSave 持久化编辑后退出）+ `/api/observe`（GET 读 observed 名单 / POST 收 Claude Code PreToolUse hook 上报，**始终 200 非阻塞**）+ **桌面 `/api/scan/*`**（`pick` 弹文件夹选择器存 `pendingDir` 立即返 `{dir}` 供前端回显路径 / `run` 在 per-scan cancel ctx 跑管线返 StudioData / `cancel` 中断在跑的 run；仅 `OnScanPick`/`OnScanRun`/`OnScanCancel` 注入时注册，拆两段让前端扫描前显示路径 + Stop 按钮）+ **桌面 `/api/auth/*`**（status/login/poll/logout，仅当 Options 注入 `OnAuthState`/`OnLogin`/`OnLoginPoll`/`OnLogout` 回调时注册 —— CLI 不注入故无这些路由，`agent edit` 无感；配合 `StudioData.Desktop` + studio.html 顶部登录条）+ **桌面流式 `/api/chat`**（仅 `OnChat` 注入时注册；POST `ChatRequest` → 转发 conductor `/chat` SSE → 每帧 `data: <raw>\n\n` + `http.Flusher` 透传回前端，**本包首个非 writeJSON 流式 handler**；chat client 在 cmd 层经 `OnChat` 注入，webstudio 只透传不 import chat/deploy，守解耦纪律）+ **桌面流式 `/api/assistant`**（仅 `OnAssistant` 注入时注册；POST `AssistantRequest` → 转发官方 Studio 助手 agent 的 conductor `/chat` SSE 透传，同 /api/chat 形态；`agent_id` 由 Go 侧 `resolveOfficialAssistant` 解析不进前端）+ **桌面 `/api/open-external`**（仅 `OnOpenExternal` 注入时注册；POST `{url}` → Go 侧 `openBrowser` 开系统浏览器 —— webview 吞 `target=_blank`/`window.open`，前端外链点击经此桥接）+ **桌面 `/api/skill-validate` + `/api/skill-fix`**（仅 `OnSkillValidate`/`OnSkillFix` 注入时注册；validate 无 body → 遍历当前草稿 custom_local skill 读各自 SKILL.md 返 `[]SkillValidation`{path/dir_name/has_name/has_description}，fix POST `SkillFix`{path,name?,description?} 写回 frontmatter —— 把 deploy 层 frontmatter gate 前移到 Skills 步行内校验 + 一键补全；path 必属已扫描 candidate 防越权写盘）+ **桌面多项目 `/api/project/{switch,remove,rescan}`**（仅 `OnProjectSwitch`/`OnProjectRemove`/`OnProjectRescan` 注入时注册；switch/remove 收 `{dir}` 切换/移除当前项目，rescan 无 body 对当前项目跑 full scan 返 StudioData；CLI 不注入故路由不存在，与 scan/chat 同隔离）。`OnSave`/`OnDeploy` 回调注入，`OnReady(port)` 在 serve 后阻塞前回调（供 cmd 的 `--observe` 在起 claude 前写 hook settings）；包内 `observed`（RWMutex 守护的 skill/MCP server 运行时集合）+ `skillNameFrom`(R2 防御取名：skill→name→首字符串) / `mcpServerOf`(拆 `mcp__server__tool`)。`buildMux` 抽出供 httptest；`Serve` 阻塞至 deploy 成功或 done。
- **logo.png** — AskDAO 品牌 logo（512×512 RGBA），go:embed 进二进制，header 左上角 30px 圆角显示，与 askdao.ai topbar 一致。
- **api.go** — `StudioData`（含 `Observe` flag + `Desktop` flag（桌面 app 会话开关，前端据此条件渲染助手侧栏/测试聊天等桌面专属块；CLI `edit` 不置=默认 false，行为无感）+ `Icons` avatar 网格数据 + `ModelCatalog`（第二步模型档 `[]types.ModelClassEntry`，cmd 层从 conductor `/cli/model-classes` 拉取注入渲染，前端零硬编码 model id）+ **`Projects []ProjectSummary`（桌面多项目切换器列表，`omitempty` → CLI 恒 nil 隐形）+ `Lightweight`（轻量 yaml 载入项目 det=nil 标记，前端 `collect()` 据此跳过 skills/mcp 重建，防清空 yaml 已声明技能）**）/ `ProjectSummary`（切换器行：dir/name/current/deployed/deployed_name（改名漂移可见）/agent_id/version/last_deployed_at/missing）/ `SkillCandidate` / `MCPCandidate` / `ObservedData`（`GET /api/observe` 的 `skills`+`mcp_servers` 名单）/ `DeployResult`（部署结构化结果：message + group_link + agent_id + created，group_link 独立字段供成功卡片渲染可点击按钮）/ `ChatRequest`（桌面 `/api/chat` body：message + agent_id + session_id，透传 conductor `/chat`）/ `AssistantRequest`（桌面 `/api/assistant` body：message + session_id + 可选 context；**无 agent_id** —— Go 侧解析官方 Studio 助手 agent）/ `SkillValidation`（`/api/skill-validate` 结果项 path/dir_name/has_name/has_description，前端据此在 Skills 步行内标红缺 frontmatter 的 skill）/ `SkillFix`（`/api/skill-fix` body path + name?/description?，空字段不动）/ `BuildStudioData(spec, det, harness, restorePrior)`。把 spec 草稿 + detection 候选摊平成前端 JSON。**勾选态二选一**：`restorePrior=true`（编辑已有 yaml）以 `spec.skills`/`mcp_servers` 为唯一真相 —— 忠实回显 KOL 上次选择（含默认本会漏掉的 stdio MCP），恢复表 key 镜像前端 `collect()` 序列化口径（builtin→id / custom_local→path / mcp→name）；`restorePrior=false`（全新草稿）走**默认策略**：project skill 全勾 / user(全局) skill opt-in 不勾 / implied builtin 勾 / Anthropic 兼容 MCP 勾、stdio 不勾。`Observe` 由 cmd 的 `--observe` 置位，前端据此显示观测区（叠加在勾选态之上，不改静态快照）。
- **theme.go** — `ThemeToken` / `Palette`(8 token) / `Categories` / `DefaultThemeForCategory` / `DefaultAvatarForCategory`（category→lucide icon 兜底，对称 `categoryDefault`，未知→`icon:bot`；值必在 `AvatarIcons` whitelist，`webstudio_test` 守漂移）。色板 token 真相源；`metadata.theme_color` 存 **token 名非 hex**，CLI 默认 + 服务端透传 token（不碰 hex）+ 订阅者端按同一 token→hex 契约渲染 Group 页品牌色；改 token/hex 端到端同步（CI diff 校验）。
- **icons.go** — `IconDef` / `AvatarIcons`(~66 lucide icon 子集，inner-SVG)。avatar 网格 picker 数据，由一次性脚本 `gen-icons.mjs` 从 lucide-react 提取 inner-SVG（24x24 viewBox）。name 集**与服务端/订阅者端的 avatar icon whitelist 保持同一契约，端到端同步**（避 lucide deprecated alias，用 canonical name 如 chart-column/chart-line/wand-sparkles）。前端包 `<svg stroke=currentColor>` 染色，avatar 圆背景复用 theme_color。
- **studio.html** — 自包含单页（inline CSS + vanilla JS，无构建/框架；字体走 Google Fonts CDN，离线 fallback 系统字体）。**向导式 4 步**（左侧竖直 stepper：Identity → Persona → Skills & Tools → Review & Deploy；圆圈 pending/active/done 三态 + 连接线）。视觉对齐 **AskDAO Kami Design System**（暖羊皮纸底 #f5f4ed + 品牌墨蓝 + Newsreader serif 标题/正文 + Inter sans 标签按钮 + ring-first 阴影 + 4pt 间距）。**亮点：工作台 accent 实时跟随 KOL 选的 theme_color**（呼应 Kami `data-agent-tone`，所见即所得预览订阅者 Group 页色调）。布局：**全宽 topbar**（屏幕左上角 AskDAO logo + "AskDAO Agent" wordmark，右上 project chip）+ 主体/footer **限宽 920 居中聚焦**（宽屏不发散，stepper 与按钮距离合理）+ aside stepper sticky。**信心度徽标**：LLM 推荐的字段（visibility / model_class / system_prompt / **display_name / description / category / theme_color / avatar** —— AI 生成字段全覆盖）旁内联 `confBadge`——一个 AI 评分 chip（绿 ≥0.8 / 琥珀 ≥0.5 / 红 <0.5 按 confidence），hover 即时弹**自定义 tooltip** 显推荐理由——挂在 `<body>` 的 fixed `#tip` **单节点**（JS 事件委托定位 + 下方空间不够则上翻），深色卡片 + 13px sans 大字；既避开原生 `title` 的延迟/模糊，也避开 CSS 伪元素被 card stacking-context / `.wrap` 滚动 overflow 裁剪的问题。穿插在设置处而非堆在 Review 末尾。`confBadge(...keys)` 按关键词匹配 `data.reasoning` 的 decision。各 step：① Identity（name + **display_name(订阅者展示名)** + **多行 description textarea** + category + **output language select（f_lang：Auto·follows the user / 中文 / English，写 `metadata.language` ISO 639-1，''=通用不落 yaml；Review 步同步展示）**；Theme color / **Avatar** / Visibility 各独立 card（常用项前置，Visibility 默认 private 移至最底） —— Avatar = icon 网格 picker（`data.icons`），预览圆复用 theme_color 染色（`renderAvatar`/`pickAvatar`/`renderIconGrid`/`avatarInner`，pickTheme 时联动重染），默认「Aa 首字母」选项）② Persona（**Model 选择器 3 档 `.seg` 药丸从 `data.model_catalog`（conductor `/cli/model-classes`）渲染，非硬编码表**；`pickModelClass` 写 `model_class` + 刷新 `.model-hint` caption「友好名 <span mono>(raw model_id)</span> · 成本」；`collect` 从 catalog 按 model_class 查 `model_id` 填 `model_preferences[0]`（离线兜底目录无 id→留空，conductor 从 model_class 兜底解析）；`modelEntry`/`modelHintHTML`/`reviewModelHTML` 目录查询单源 + 信心度 / system_prompt）③ Skills & Tools（**Skills / MCP servers / Secrets 三 Tab**；Skills/MCP 按 scope 分组：当前目录/用户级全局/inferred builtin，全局默认不勾，标 scope + **harness 准确名**（`HARNESS_NAME`：claude→Claude Code / codex→OpenAI Codex / cursor→Cursor / cowork→Claude Cowork）+ origin badge，多 harness 在 scope 组内按 harness 聚类排序；**Secrets = vault_hints 凭证声明**（tick 纳入模型，对齐 Skills/MCP）：扫描凭证行只读 name(mono) + required/optional 标签 + 单 tick（默认全勾、取消不纳入），底部「+ Add credential」手动补录行（输入 + tick + × 删）；required/optional 由每行自带 `required`（scanner 分类，`initVault` 还原）后台自动拆桶、用户只 tick，"值永不在此填"，`collect` 只收勾选的、按 required 拆 required/optional_credentials）④ Review 汇总（profile + avatar 预览圆**浮 card 右上角**（`.rev-av-corner`，与 Name/Display 平行，不重复图标名）+ system_prompt 全文(限高滚动 `.rev-prompt`) + Category 不再重复 theme 色块（avatar 圆已示色）+ **列出选中的 skill/MCP/secret 具体名称 chips**，非仅计数，供最后逐项确认）+ Deploy/Save&finish。一次性渲染全部 step（display 切换 + rise 动画），`collect()` 跨 step 保值并把勾选项重组进 `spec.skills`/`spec.mcp_servers`/`spec.vault_hints` 后 POST。**observe 叠加层**（`data.observe` 为真时 Skills 步顶 `observeBar` 横幅）：`startObservePolling` 每 2s GET `/api/observe`，`applyObserved` 给真正激活的 skill/MCP item 加绿底 + 「✓ used」badge（**不改勾选态，默认全勾保持为安全网**），`narrowToObserved`(「Keep only observed」按钮)由 KOL 主动把未观测到的取消勾（builtin 不动——无法经 Skill 工具激活）。**统一结果对话框**（`showDialog`/`closeDialog`/`overlayClick`/`copyGroupLink` + `.overlay`/`.done-card`）：所有 Save/Deploy 结果都弹居中 overlay 卡片，不再用 footer 角落小字（13px 易被长 message 撑变形）。两维度：`kind`=success（绿 ✓）/ error（红 ✕，`.done-check.err`）；`terminal`=true（Deploy / Save & finish）：**CLI 浏览器**（server 随 deploy/done 退出、页面变死页）显「You can close this tab」无 dismiss；**桌面 app**（`data.desktop`，server.go Handler 常驻不退）始终可 dismiss（Continue 按钮 + 点遮罩 +「You can close the window, or keep editing」提示）。Deploy 含可点击 group 链接（「Open group page」新窗口 +「Copy link」复制，KOL 一键拿走订阅者入口）；`terminal`=false（Draft saved / 任何错误，server 仍活）显 dismiss 按钮（成功「Continue」/ 错误「Dismiss」）+ 点遮罩背景关闭，关闭后继续编辑。footer `status` 仅留进行中的 `Working…`（短词不撑布局）。**桌面测试聊天**（`data.desktop`）：deploy 成功卡片多一个「Test chat」按钮（`doneChatBtn`，捕获 `DeployResult.agent_id`），点开 `chatOverlay` 聊天面板 —— `sendChat` POST `/api/chat`（`{message, agent_id, session_id}`）+ `response.body.getReader()`+`TextDecoder` 逐块解析 `data:` 帧（跳 `:` 心跳，按 `.type`：`text_delta` 累加进 agent 气泡 / `text_delta_replacement` 整段替换气泡（scaffolding 改写的最终答案，对齐 ai-web `fullText=event.text`，测试聊天显示订阅者所见而非中间 raw 稿）/ `done` 取 `sdk_session_id`\|`ov_session_id` 续多轮（带 `awaiting_clarify` 则显降级提示——测试聊天不支持交互式澄清）/ `error` 标红），消息一律 `textContent` 渲染防注入；`cli_` token 不进页面（Go 侧持有）。首个前端流式消费（其余全 `fetch().json()`）。**桌面外链桥接**：顶层 document click 拦 `a[target=_blank]`（`data.desktop` 门控）→ POST `/api/open-external` → Go `openBrowser` 开系统浏览器（webview 不认 `target=_blank`/`window.open`；单点拦截覆盖 group link 及未来所有外链）；CLI 保留原生 `target=_blank`。**桌面 SKILL.md 校验**（`data.desktop`）：`load` 后（已扫描）`validateSkills` POST `/api/skill-validate`，缺 name/description 的 custom_local skill 在 Skills 步 item 内注入琥珀 `.skill-flag`（`renderSkillFlag` 照 observe `markUsed` 注入模式）+ 顶部 `.flagbar` 汇总「N skills need a complete SKILL.md」+ Re-check；一键补全 `fixSkill` POST `/api/skill-fix`（缺 name→「Use folder name」按钮 / 缺 description→输入框 Save），成功后重校验刷新；注入 DOM 全 `textContent`/`createElement`（防 skill 名/描述注入）。**桌面 AI 助手侧栏**（`data.desktop`）：右侧抽屉 `assistantDrawer` + 浮动 `assistantFab` 开关（`load` 时 desktop 显），`sendAssistant` POST `/api/assistant`（`{message, session_id}`，**无 agent_id**）逐帧消费同 `sendChat`（`text_delta` 累加 / `text_delta_replacement` 整段替换 / `done` 续 session / `error` 标红）+ **`unavailable` 帧降级静态提示**（未登录 / 官方助手未配置 → 不误聊到自己的 agent）；全 `textContent` 防注入。复用 `.chat-*` 样式，与测试聊天区分（那个测活 agent，这个帮**建** agent —— 解释字段 / 建议 prompt / 起草 SKILL.md 描述 / 诊断部署报错）。

## 设计约束

- **回调解耦**：webstudio 只管 HTTP + 序列化；写 yaml / deploy 业务由 cmd 注入 `OnSave`/`OnDeploy`，避免依赖 pipeline/deploy（单向依赖：cmd → webstudio）。
- **本地隐私**：绑 `127.0.0.1`（非 `localhost`，IPv4 确定避免 IPv6 ECONNREFUSED），数据只在本地，不上传文件内容。
- **无构建链**：studio.html 是 `go:embed` 静态资源，inline CSS/JS；字体走 Google Fonts CDN（联网时与品牌站视觉一致，离线 fallback Georgia/system），不引 npm/打包；契合项目 stdlib-only + 单二进制哲学。
- **风格对齐品牌站**：复用 AskDAO Kami Design System 的 token（color/font/radius/shadow/spacing），与品牌站视觉一脉相承；非独立创意美学。
- **scope 分组 + 默认勾选**：见 api.go —— user(全局)skill 默认不勾，避免 KOL 全局 skill 库被自动打包进每个 agent。
- **observe 是叠加层不是唯一真相**：`--observe` 观测到的项只做证据高亮 + 一键收窄建议，默认全勾保持为安全网；`observed` 是 server 运行时状态（非 `BuildStudioData` 静态快照），前端轮询叠加。临时 hook settings 的写入/合并/清理（零残留三件套）在 [../observe/CLAUDE.md](../observe/CLAUDE.md)；cmd 经 `OnReady(port)` 串联两者。
- **输入校验**（`validateStep`/`validateAll`/`showErrors`/`clearErrors`）：必填 = name + model_class（后端 required）+ display_name + description + system_prompt + vault secret name（产品要求）；name slug 格式 `^[a-z0-9]+(-[a-z0-9]+)*$`（≤64）。**Next 翻页拦当前 step + Deploy/finish 全量拦并跳第一个错误 step**；Save draft 宽松（仅 name 非空）。字段下 `.field-err` 红字 + 输入 `.invalid` 红框。对齐服务端校验契约；受控选择器（visibility/model radio、theme/avatar/category picker）天然合法不校验。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
